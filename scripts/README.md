# scripts Text

- `windows/`: Windows Text(`.bat`). 
- `ops/`: Text, Text, Cloudflare Text(`.ps1`). 
- `maintenance/`: Text. 
- `migrations/`: Text. 
- `tools/`: Text(Text, Text). 
- `manual_checks/`: Text. 

Text: 
- Text `Text.bat`, `Text.bat`, `CloudflareText.bat`, `restart-web.ps1`, `share-cloudflare-quick-tunnel.ps1` Text. 
- `scripts/` Text Python wrapper, Text, Text. 
