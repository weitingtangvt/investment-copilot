# Investment Assistant - Sanitized English Copy

This copy was generated for sharing. It removes private runtime data, broker uploads, cached market data, screenshots, logs, historical docs, tests, and personal holdings.

Chinese UI/source text has been replaced with English labels or neutral placeholders. API keys and local user paths are redacted.

Run with:

```powershell
pip install -r requirements.txt
python web/app.py
```
